public class Ball
{
//    private int radius;
      
  //  Ball() // Ball ball = new Ball();
    {
    //    this.radius = 1;
    }

    //Ball(int radius) // Ball ball = new Ball(5);
    {
      //  this.radius = radius;
    }
}
